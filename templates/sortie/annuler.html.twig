{% extends 'base.html.twig' %}

{% block title %}Annuller Sortie{% endblock %}

{% block body %}
    <h1>Ajouter une ville</h1>

    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button type="submit">Save</button>
    {{ form_end(form) }}
{% endblock %}